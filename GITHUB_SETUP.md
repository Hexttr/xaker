# 🔗 Настройка GitHub репозитория

## Текущий статус

Репозиторий готов к пушу в GitHub, но нужно настроить remote.

## 📋 Шаги для пуша в GitHub

### 1. Проверьте, что репозиторий инициализирован

```powershell
git status
```

### 2. Добавьте remote (если еще не добавлен)

```powershell
git remote add origin https://github.com/Hexttr/xaker.git
```

Или если уже существует:

```powershell
git remote set-url origin https://github.com/Hexttr/xaker.git
```

### 3. Проверьте remote

```powershell
git remote -v
```

Должно показать:
```
origin  https://github.com/Hexttr/xaker.git (fetch)
origin  https://github.com/Hexttr/xaker.git (push)
```

### 4. Добавьте все файлы и закоммитьте

```powershell
git add -A
git commit -m "Добавлены инструкции по установке и развертыванию"
```

### 5. Запушьте в GitHub

```powershell
git push -u origin master
```

Или если ваша основная ветка называется `main`:

```powershell
git push -u origin main
```

## ⚠️ Если репозиторий на GitHub пустой

Если репозиторий на GitHub пустой (как указано в веб-поиске), используйте:

```powershell
git push -u origin master --force
```

**Внимание:** Используйте `--force` только если вы уверены, что хотите перезаписать удаленную ветку!

## ✅ После успешного пуша

После успешного пуша, на другой машине можно будет:

```powershell
git clone https://github.com/Hexttr/xaker.git
cd xaker
# Следовать инструкции из INSTALL.md
```

## 📝 Что будет в репозитории

### Код проекта:
- ✅ Backend код (TypeScript)
- ✅ Frontend код (React)
- ✅ Конфигурация проекта
- ✅ PowerShell скрипты

### Документация:
- ✅ README.md
- ✅ INSTALL.md (новая!)
- ✅ DEPLOYMENT.md (новая!)
- ✅ Все остальные MD файлы с документацией

### Что НЕ будет (правильно):
- ❌ `node_modules/` - зависимости (в .gitignore)
- ❌ `backend/.env` - конфиденциальные данные (в .gitignore)
- ❌ `shannon/` - клонируется отдельно (в .gitignore)
- ❌ `backend/temp-repo/` - временные файлы (в .gitignore)


