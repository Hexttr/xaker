# Инструкция по запуску проекта

## ✅ Быстрый запуск (рекомендуется)

### Вариант 1: Одно окно
```powershell
cd C:\Xaker
npm run dev
```

### Вариант 2: Два окна (для просмотра логов)

**Окно 1 - Backend:**
```powershell
cd C:\Xaker\backend
npx tsx src/server.ts
```

**Окно 2 - Frontend:**
```powershell
cd C:\Xaker\frontend
npm run dev
```

## 🔍 Проверка работы

### Backend API
Откройте в браузере: **http://localhost:3000/api/health**

Должен вернуть:
```json
{"status":"ok","timestamp":"..."}
```

### Frontend
Откройте в браузере: **http://localhost:5173**

Должна открыться страница с заголовком "Xaker - AI Penetration Tester"

## ⚠️ Если backend не отвечает

1. **Проверьте, что процесс запущен:**
```powershell
Get-Process node
netstat -ano | findstr ':3000'
```

2. **Попробуйте упрощенный сервер:**
```powershell
cd C:\Xaker\backend
npx tsx src/server-simple.ts
```

3. **Откройте URL напрямую в браузере:**
   - http://localhost:3000/api/health
   - http://127.0.0.1:3000/api/health

4. **Проверьте логи в консоли** - там будут видны ошибки

## 🛑 Остановка серверов

Нажмите `Ctrl+C` в консоли, где запущен сервер

Или остановите все процессы Node:
```powershell
Get-Process node | Stop-Process -Force
```

## 📝 Примечания

- Frontend работает на порту **5173**
- Backend работает на порту **3000**
- Если порты заняты, измените их в `.env` и `vite.config.ts`



