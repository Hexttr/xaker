# 🧪 Тестирование интеграции MiroMind

## Текущий статус

✅ **Ollama установлен и запущен**  
✅ **Backend настроен на использование MiroMind**  
⏳ **Модель загружается** (llama3.1:8b)

## Проверка модели

```powershell
curl http://localhost:11434/api/tags
```

Должна быть модель `llama3.1:8b`

## Тест генерации отчета

1. Убедитесь, что backend запущен
2. Откройте http://localhost:5173
3. Выберите пентест (например, тест #5)
4. Нажмите "Сгенерировать отчет"
5. В логах должно быть: `🧠 Использую MiroMind для генерации отчета`

## Тест Shannon

1. Создайте новый пентест
2. Запустите его
3. В логах должно быть: `🧠 Используется MiroMind: http://localhost:11434`

## Если модель еще загружается

Модель llama3.1:8b загружается в фоне. Проверьте статус:

```powershell
& "$env:LOCALAPPDATA\Programs\Ollama\ollama.exe" list
```

Когда модель появится в списке - можно тестировать!

