# 🚀 Полная инструкция по установке MiroThinker-8B

## Ваша конфигурация
- ✅ **32GB RAM** - отлично
- ✅ **RTX 5060 8GB VRAM** - идеально для MiroThinker-8B
- ✅ **Ryzen CPU** - хорошо

## Рекомендация: MiroThinker-8B

Для вашей конфигурации идеально подходит **MiroThinker-8B**:
- Поместится в 8GB VRAM полностью
- Быстрая генерация (~1-3 сек на запрос)
- Хорошее качество ответов

---

## Вариант 1: Установка через Ollama (РЕКОМЕНДУЕТСЯ для Windows)

### Шаг 1: Установить Ollama

1. Скачайте Ollama: https://ollama.com/download/windows
2. Установите (просто запустите установщик)
3. Проверьте установку:
   ```powershell
   ollama --version
   ```

### Шаг 2: Загрузить модель MiroThinker-8B

```powershell
ollama pull mirothinker-8b
```

**Время загрузки:** 10-30 минут (модель ~16GB)

### Шаг 3: Запустить Ollama сервер

```powershell
ollama serve
```

Ollama будет доступен на `http://localhost:11434`

### Шаг 4: Проверить работу

```powershell
curl http://localhost:11434/v1/models
```

Должен вернуть список моделей, включая `mirothinker-8b`

### Шаг 5: Настроить Xaker

В `backend/.env`:
```env
USE_MIROMIND=true
MIROMIND_API_URL=http://localhost:11434/v1
MIROMIND_MODEL=mirothinker-8b
```

---

## Вариант 2: Установка через SGLang (для продвинутых)

### Требования
- Python 3.10+
- CUDA 11.8+ (для GPU)
- 8GB+ VRAM

### Установка

```powershell
cd miromind
.\venv\Scripts\Activate.ps1
pip install sglang[all]
```

### Загрузка модели

```python
from sglang import Runtime, Engine

runtime = Runtime(
    model_path="MiroMindAI/MiroThinker-v1.0-8B",
    tp_size=1  # Для одной GPU
)
```

### Запуск сервера

```python
python -m sglang.launch_server \
  --model-path MiroMindAI/MiroThinker-v1.0-8B \
  --port 8000 \
  --host 0.0.0.0
```

---

## Использование с Shannon

### Текущая ситуация

Shannon использует `ANTHROPIC_API_KEY` напрямую через переменные окружения. Для работы через MiroMind:

1. **Автоматическое переопределение** (уже реализовано):
   - Если `USE_MIROMIND=true`, система устанавливает `ANTHROPIC_API_BASE_URL`
   - Многие SDK поддерживают эту переменную

2. **Если не работает автоматически**:
   - Нужно модифицировать код Shannon
   - Или использовать прокси-сервер, который перенаправляет запросы

### Настройка для Shannon

В `backend/.env`:
```env
USE_MIROMIND=true
MIROMIND_API_URL=http://localhost:11434/v1
ANTHROPIC_API_KEY=not-needed  # Для MiroMind не нужен реальный ключ
```

Shannon автоматически будет использовать MiroMind endpoint через `ANTHROPIC_API_BASE_URL`.

---

## Проверка работы

### 1. Проверить MiroMind сервер

```powershell
curl http://localhost:11434/v1/models
```

### 2. Тест генерации отчета

1. Запустите backend
2. Создайте пентест
3. Сгенерируйте отчет
4. В логах должно быть: `🧠 Использую MiroMind для генерации отчета`

### 3. Тест Shannon

1. Запустите пентест через Shannon
2. В логах должно быть: `🧠 Используется MiroMind: http://localhost:11434`

---

## Производительность

### Ожидаемые показатели (MiroThinker-8B на RTX 5060):

- **Генерация отчета:** 30-60 секунд
- **Токенов в секунду:** ~20-40 tokens/sec
- **Использование VRAM:** ~6-7GB
- **Использование RAM:** ~8-12GB

---

## Устранение проблем

### Проблема: Модель не загружается

**Решение:**
- Проверьте свободное место на диске (нужно ~20GB)
- Проверьте интернет-соединение
- Попробуйте: `ollama pull mirothinker-8b --verbose`

### Проблема: Недостаточно VRAM

**Решение:**
- Используйте quantization: `ollama pull mirothinker-8b:q4_0` (4-bit, ~4GB)
- Или используйте CPU (медленнее, но работает)

### Проблема: Shannon не использует MiroMind

**Решение:**
- Проверьте, что `USE_MIROMIND=true` в `.env`
- Проверьте, что `MIROMIND_API_URL` указан правильно
- Перезапустите backend
- Проверьте логи Shannon на наличие `ANTHROPIC_API_BASE_URL`

---

## Следующие шаги

1. ✅ Установить Ollama
2. ✅ Загрузить модель: `ollama pull mirothinker-8b`
3. ✅ Запустить: `ollama serve`
4. ✅ Настроить `.env`
5. ✅ Перезапустить backend
6. ✅ Протестировать генерацию отчетов
7. ✅ Протестировать Shannon

**Готово!** 🎉

