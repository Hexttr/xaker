# 🧠 Использование MiroMind в пентестере Xaker

## Быстрый старт

### 1. Установка (уже выполнено)

```powershell
cd miromind
.\install.ps1
```

### 2. Запуск сервера

```powershell
.\start-server.ps1
```

Сервер запустится на `http://localhost:8000`

### 3. Настройка в проекте

Добавьте в `backend/.env`:

```env
USE_MIROMIND=true
MIROMIND_API_URL=http://localhost:8000/v1
MIROMIND_MODEL=mirothinker-8b
```

### 4. Перезапуск backend

```powershell
cd backend
npm run dev
```

## Переключение между MiroMind и Claude

В `backend/.env`:

- `USE_MIROMIND=true` - использовать MiroMind (локально)
- `USE_MIROMIND=false` - использовать Claude API (облако)

## Проверка работы

```powershell
cd miromind
.\test-connection.ps1
```

## Примечания

- **Текущий сервер** - это тестовый сервер для проверки интеграции
- **Для реальной работы** нужно установить SGLang/vLLM и загрузить модель MiroThinker
- **Документация:** https://github.com/MiroMindAI/MiroThinker

## Преимущества MiroMind

✅ Не нужен баланс Anthropic  
✅ Не нужен VPN  
✅ Полный контроль данных  
✅ Нет лимитов запросов  

