# 🚀 Настройка реальной модели MiroThinker

## Ваши ресурсы
- ✅ **32GB RAM** - отлично
- ✅ **RTX 5060 8GB VRAM** - идеально для MiroThinker-8B
- ✅ **Ryzen CPU** - хорошо

## Рекомендуемая модель

**MiroThinker-8B** - идеально подходит для вашей конфигурации:
- Поместится в 8GB VRAM
- Быстрая генерация
- Хорошее качество ответов

## Вариант 1: Установка через Ollama (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Установить Ollama
1. Скачайте: https://ollama.com/download
2. Установите Ollama
3. Проверьте: `ollama --version`

### Шаг 2: Загрузить модель
```powershell
ollama pull mirothinker-8b
```
Это займет 10-30 минут (модель ~16GB)

### Шаг 3: Запустить сервер
```powershell
ollama serve
```

Ollama будет доступен на `http://localhost:11434`

### Шаг 4: Настроить Xaker
В `backend/.env`:
```env
USE_MIROMIND=true
MIROMIND_API_URL=http://localhost:11434/v1
MIROMIND_MODEL=mirothinker-8b
```

## Вариант 2: Прямая установка (альтернатива)

Если Ollama не подходит, можно установить напрямую:

```powershell
cd miromind
.\install.ps1
python download-model.py
```

## Проверка работы

```powershell
curl http://localhost:11434/v1/models
```

## Использование с Shannon

Shannon использует `ANTHROPIC_API_KEY` напрямую. Для работы через MiroMind нужно:
1. Установить переменную `ANTHROPIC_API_BASE_URL` (если Shannon поддерживает)
2. Или модифицировать `shannon.service.ts` для переопределения endpoint

**Примечание:** Shannon может требовать изменений в своем коде для работы с локальным endpoint.

