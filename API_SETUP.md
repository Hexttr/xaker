# 🔑 Настройка API для Shannon

## 🤖 Какая ИИ используется?

**Shannon использует Claude от Anthropic** - это продвинутая языковая модель, аналогичная ChatGPT, но от компании Anthropic.

### Что такое Claude?
- **Claude** - это AI-ассистент от Anthropic
- Используется для анализа кода, поиска уязвимостей, генерации отчетов
- Shannon использует **Claude Agent SDK** для автономного пентестинга

## 🔑 Какой API ключ нужен?

Нужен **Anthropic API Key** от компании Anthropic.

### Два варианта аутентификации:

1. **ANTHROPIC_API_KEY** (рекомендуется)
   - Прямой API ключ
   - Проще в использовании
   - Формат: `sk-ant-api03-...`

2. **CLAUDE_CODE_OAUTH_TOKEN** (альтернатива)
   - OAuth токен от Claude Console
   - Для использования Claude Code

## 📝 Как получить API ключ

### Шаг 1: Зарегистрироваться
1. Перейдите на https://console.anthropic.com
2. Создайте аккаунт или войдите

### Шаг 2: Получить API ключ
1. В консоли перейдите в **API Keys**
2. Нажмите **Create Key**
3. Скопируйте ключ (он начинается с `sk-ant-api03-`)
4. ⚠️ **Сохраните его сразу** - больше не будет показан!

### Шаг 3: Пополнить баланс
- Claude API платный
- Нужно добавить кредиты на счет
- Примерная стоимость: ~$50 за полный пентест (по информации из README)

## ⚙️ Настройка в проекте

### 1. Добавить ключ в `.env`

Откройте `backend/.env` и добавьте:

```env
ANTHROPIC_API_KEY=sk-ant-api03-ваш_ключ_здесь
CLAUDE_CODE_MAX_OUTPUT_TOKENS=64000
```

### 2. Перезапустить backend

После добавления ключа перезапустите backend:
```bash
# Остановите текущий (Ctrl+C)
# Запустите заново
npm run dev
```

## 💰 Стоимость

Согласно документации Shannon:
- **Примерная стоимость:** ~$50 USD за полный пентест
- Зависит от сложности приложения
- Используется модель Claude 4.5 Sonnet

## 🔄 Альтернативы (если нет API ключа)

Если нет API ключа, система автоматически использует **симуляцию**:
- Пентесты будут выполняться с реалистичными логами
- Реальных результатов не будет
- Подходит для тестирования интерфейса

## 📊 Как работает

1. **Shannon запускается** с вашим API ключом
2. **Claude анализирует код** приложения
3. **Claude ищет уязвимости** используя свои знания
4. **Claude генерирует отчеты** с найденными проблемами
5. **Результаты сохраняются** в веб-интерфейсе

## ✅ Проверка настройки

После добавления ключа:
1. Создайте пентест в веб-интерфейсе
2. Запустите его
3. В логах должно быть: `🚀 Начинается пентест через Shannon...`
4. Если видите реальные логи от Shannon - все работает!

## 🔒 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл в git
- Не делитесь API ключом
- Используйте разные ключи для разработки и продакшена

## 📚 Дополнительная информация

- **Anthropic Console:** https://console.anthropic.com
- **Документация Claude API:** https://docs.anthropic.com
- **Shannon README:** см. `shannon/README.md`

