# 🚀 Руководство по развертыванию Xaker

## ✅ Что уже запушено в репозиторий

### Основные файлы:
- ✅ Код backend (TypeScript)
- ✅ Код frontend (React)
- ✅ Конфигурация проекта
- ✅ PowerShell скрипты для запуска
- ✅ Документация

### Что НЕ запушено (и правильно):
- ❌ `node_modules/` - зависимости (устанавливаются через npm)
- ❌ `backend/.env` - конфиденциальные данные (API ключи)
- ❌ `shannon/` - подмодуль, клонируется отдельно
- ❌ `backend/temp-repo/` - временные файлы пентестов
- ❌ Скомпилированные файлы `dist/`

---

## 📦 Быстрое развертывание на новой машине Windows

### Вариант 1: Автоматическая установка (рекомендуется)

Создайте файл `install.ps1` и выполните:

```powershell
.\install.ps1
```

### Вариант 2: Ручная установка

См. подробную инструкцию в `INSTALL.md`

---

## 🔍 Проверка готовности к развертыванию

### Что должно быть в репозитории:

```powershell
# Проверка структуры
Test-Path package.json              # ✅ Должен быть
Test-Path backend\package.json      # ✅ Должен быть
Test-Path frontend\package.json     # ✅ Должен быть
Test-Path .gitignore                # ✅ Должен быть
Test-Path INSTALL.md                # ✅ Должен быть
Test-Path start-all.ps1             # ✅ Должен быть
```

### Что НЕ должно быть в репозитории:

```powershell
# Эти папки/файлы должны быть в .gitignore
Test-Path node_modules              # ❌ Не должно быть
Test-Path backend\.env              # ❌ Не должно быть
Test-Path shannon                   # ❌ Не должно быть
```

---

## 📋 Чек-лист для развертывания

### Перед развертыванием:

- [ ] Репозиторий клонирован
- [ ] Node.js установлен (v18+)
- [ ] Git установлен
- [ ] PowerShell 7+ установлен

### Установка:

- [ ] Зависимости установлены (`npm run install:all`)
- [ ] Shannon клонирован отдельно
- [ ] Shannon собран (`npm run build`)
- [ ] mcp-server собран
- [ ] Создан `backend/.env`
- [ ] Настроен API ключ (если нужно)

### Проверка:

- [ ] Backend запускается (`start-backend.ps1`)
- [ ] Frontend запускается (`start-frontend.ps1`)
- [ ] API отвечает (`http://localhost:3000/api/health`)
- [ ] Веб-интерфейс открывается (`http://localhost:5173`)

---

## 🌐 Развертывание на другой машине Windows

### Пошаговая инструкция:

1. **Клонирование репозитория**
   ```powershell
   git clone https://github.com/Hexttr/xaker.git
   cd xaker
   ```

2. **Установка зависимостей**
   ```powershell
   npm run install:all
   ```

3. **Клонирование Shannon**
   ```powershell
   cd ..
   git clone https://github.com/KeygraphHQ/shannon.git xaker\shannon
   cd xaker\shannon
   npm install
   npm run build
   cd mcp-server
   npm install
   npm run build
   cd ..\..\xaker
   ```

4. **Настройка окружения**
   ```powershell
   cd backend
   # Создайте .env файл (скопируйте из .env.example или создайте вручную)
   cd ..
   ```

5. **Запуск**
   ```powershell
   .\start-all.ps1
   ```

**Готово!** Откройте http://localhost:5173

---

## 🔄 Обновление до последней версии

```powershell
# Обновление кода
git pull origin main

# Обновление зависимостей
npm run install:all

# Пересборка (если нужно)
cd backend
npm run build
cd ..
```

---

## 📝 Важные замечания

### Что нужно вручную на каждой машине:

1. **Создать `backend/.env`** - с вашими API ключами
2. **Клонировать Shannon** - это отдельный репозиторий
3. **Установить зависимости** - `npm install`

### Что автоматически не устанавливается:

- Node.js (нужно установить вручную)
- Git (нужно установить вручную)
- Anthropic API ключ (нужно получить и добавить в `.env`)

---

## ⚡ Быстрая команда для новой машины

Скопируйте и выполните всю эту команду:

```powershell
# Клонирование и установка Xaker
cd C:\; git clone https://github.com/Hexttr/xaker.git; cd xaker; npm run install:all

# Клонирование и сборка Shannon
cd ..; git clone https://github.com/KeygraphHQ/shannon.git xaker\shannon; cd xaker\shannon; npm install; npm run build; cd mcp-server; npm install; npm run build; cd ..\..\xaker

# Создание .env (нужно отредактировать вручную)
cd backend; @"
PORT=3000
FRONTEND_URL=http://localhost:5173
ANTHROPIC_API_KEY=your_api_key_here
USE_SIMULATION=false
"@ | Out-File -FilePath .env -Encoding utf8; cd ..

# Готово! Теперь запустите: .\start-all.ps1
Write-Host "`n✅ Установка завершена! Теперь:" -ForegroundColor Green
Write-Host "1. Отредактируйте backend\.env и добавьте ваш API ключ" -ForegroundColor Yellow
Write-Host "2. Запустите: .\start-all.ps1" -ForegroundColor Yellow
```

---

## 🐛 Если что-то не работает

1. Проверьте логи в окнах PowerShell
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порты 3000 и 5173 свободны
4. См. раздел "Решение проблем" в `INSTALL.md`

---

**Готово к развертыванию!** 🎉


