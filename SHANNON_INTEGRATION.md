# Интеграция с Shannon

## ✅ Что сделано

1. **Создан сервис интеграции** (`backend/src/services/shannon.service.ts`)
   - Проверка доступности Shannon
   - Запуск Shannon как дочерний процесс
   - Перехват логов (stdout/stderr)
   - Обработка завершения и ошибок
   - Fallback на симуляцию если нет API ключа

2. **Shannon клонирован** в `../shannon`

## 🔧 Настройка

### 1. Установить ANTHROPIC_API_KEY

Добавьте в `backend/.env`:
```
ANTHROPIC_API_KEY=your_actual_api_key_here
```

Получить ключ можно на: https://console.anthropic.com

### 2. Собрать Shannon

```bash
cd ../shannon
npm install
npm run build
```

### 3. Подготовить репозиторий (опционально)

Shannon требует путь к репозиторию с исходным кодом. Если не указан, будет использован временный путь.

Можно указать в конфигурации пентеста:
```json
{
  "targetUrl": "https://example.com",
  "scope": ["/path/to/repo"]
}
```

## 🚀 Как работает

1. **Создание пентеста** → Пользователь указывает URL цели
2. **Запуск пентеста** → Backend вызывает `shannonService.runPentest()`
3. **Проверка** → Проверяется наличие Shannon и API ключа
4. **Запуск Shannon** → Запускается как дочерний процесс:
   ```bash
   node ../shannon/dist/shannon.js <targetUrl> <repoPath>
   ```
5. **Перехват логов** → Все stdout/stderr попадают в логи пентеста
6. **Завершение** → Статус обновляется на "completed" или "failed"

## 📝 Логи

Все логи от Shannon автоматически попадают в систему:
- `info` - обычные сообщения (stdout)
- `error` - ошибки (stderr)
- `success` - успешное завершение

## ⚠️ Важно

1. **ANTHROPIC_API_KEY обязателен** для реального выполнения
   - Без него будет использоваться симуляция
   
2. **Shannon должен быть собран** (`npm run build` в папке shannon)

3. **Репозиторий** - Shannon работает лучше с доступом к исходному коду
   - Можно указать путь в конфигурации пентеста
   - Или использовать временный путь

## 🔄 Fallback режим

Если:
- Shannon не найден
- Нет ANTHROPIC_API_KEY
- Ошибка запуска

То используется **симуляция** выполнения пентеста с реалистичными логами.

## 📊 Следующие шаги

1. ✅ Базовая интеграция готова
2. ⏳ Парсинг результатов Shannon
3. ⏳ Извлечение найденных уязвимостей
4. ⏳ Сохранение отчетов
5. ⏳ Улучшение обработки ошибок


