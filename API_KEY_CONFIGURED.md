# ✅ API ключ настроен!

## 🔑 Что сделано

1. ✅ **API ключ добавлен** в `backend/.env`
2. ✅ **Переменная окружения** `CLAUDE_CODE_MAX_OUTPUT_TOKENS=64000` установлена

## 🚀 Следующие шаги

### 1. Собрать Shannon (если еще не собрано)

```bash
cd ../shannon
npm install
npm run build
```

### 2. Перезапустить backend

**Важно:** Backend нужно перезапустить, чтобы он подхватил новый API ключ из `.env`

```bash
# Остановите текущий backend (Ctrl+C)
# Запустите заново
cd C:\Xaker
npm run dev
```

### 3. Проверить работу

1. Откройте http://localhost:5173
2. Создайте новый пентест
3. Запустите его
4. В логах должно быть: `🚀 Начинается пентест через Shannon...`
5. Если видите реальные логи от Shannon - все работает! ✅

## ⚠️ Важно

- **API ключ в .env** - это безопасно, файл в `.gitignore`
- **Не коммитьте** `.env` в git (уже настроено)
- **Не делитесь** API ключом публично

## 💰 Стоимость

- Каждый пентест будет использовать Claude API
- Примерная стоимость: ~$50 за полный пентест
- Следите за балансом на https://console.anthropic.com

## 🎯 Готово к использованию!

После перезапуска backend можно запускать **реальные пентесты** через Claude AI!








