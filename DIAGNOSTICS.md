# 🔍 Система диагностики и автоматического деплоя

## Доступные скрипты

### 1. `diagnose.ps1` - Базовая диагностика
Проверяет состояние файлов на сервере и в локальной копии.

**Использование:**
```powershell
cd C:\Xakerprod
.\diagnose.ps1
```

**Что проверяет:**
- Файлы компонентов на сервере
- Собранные файлы (JS/CSS)
- Наличие Layout, Logo, Sidebar
- Сравнение локальных и серверных файлов

### 2. `fix-and-deploy.ps1` - Автоматический деплой
Копирует файлы на сервер и пересобирает приложение.

**Использование:**
```powershell
cd C:\Xakerprod
.\fix-and-deploy.ps1
```

**Что делает:**
1. Обновляет код из Git на сервере
2. Копирует критичные файлы напрямую:
   - `Layout.tsx`
   - `Logo.tsx`
   - `Sidebar.tsx`
   - `index.css`
   - `tailwind.config.js`
3. Очищает кэш и пересобирает
4. Проверяет результат
5. Перезагружает Nginx

### 3. `deep-diagnose.ps1` - Глубокая диагностика
Проверяет детали сборки (inline стили, SVG, CSS классы).

**Использование:**
```powershell
cd C:\Xakerprod
.\deep-diagnose.ps1
```

### 4. `final-fix.ps1` - Финальное исправление
Полная проверка и пересборка с детальным отчетом.

**Использование:**
```powershell
cd C:\Xakerprod
.\final-fix.ps1
```

## Типичные проблемы и решения

### Проблема: Изменения не видны на сайте

**Диагностика:**
1. Запустите `diagnose.ps1`
2. Проверьте, что файлы на сервере совпадают с локальными
3. Проверьте, что сборка прошла успешно

**Решение:**
1. Запустите `fix-and-deploy.ps1`
2. Очистите кэш браузера (Ctrl+Shift+Del)
3. Откройте сайт в режиме инкогнито

### Проблема: Логотип не отображается

**Диагностика:**
```powershell
.\server-command.ps1 -Command "grep -c 'Pentest.*red' /opt/xaker/frontend/dist/assets/index-*.js"
```

**Решение:**
1. Убедитесь, что `Logo.tsx` существует
2. Убедитесь, что `Sidebar.tsx` импортирует Logo
3. Запустите `fix-and-deploy.ps1`

### Проблема: Сетчатый фон не виден

**Диагностика:**
```powershell
.\server-command.ps1 -Command "grep -c 'bg-grid-dark' /opt/xaker/frontend/dist/assets/index-*.css"
```

**Решение:**
1. Проверьте `index.css` - должен быть класс `.bg-grid-dark`
2. Проверьте `Layout.tsx` - должен использовать `bg-grid-dark`
3. Запустите `fix-and-deploy.ps1`

## Ручная проверка на сервере

### Проверка файлов
```bash
# На сервере
cd /opt/xaker/frontend
ls -la src/components/
cat src/components/Layout.tsx | grep -A 3 "Grid Background"
cat src/components/Logo.tsx | head -20
```

### Проверка сборки
```bash
# На сервере
cd /opt/xaker/frontend
ls -lh dist/assets/
grep -c "bg-grid-dark" dist/assets/*.css
grep -c "Pentest.*red" dist/assets/*.js
```

### Пересборка
```bash
# На сервере
cd /opt/xaker/frontend
rm -rf dist node_modules/.vite .vite
NODE_ENV=production npm run build
systemctl reload nginx
```

## Проверка в браузере

### Консоль браузера (F12)
```javascript
// Проверка сетчатого фона
document.querySelector('[data-testid="grid-background"]')

// Проверка логотипа
document.querySelector('[data-testid="logo-svg"]')

// Проверка Layout
document.querySelector('.min-h-screen.bg-black')

// Проверка всех элементов с bg-grid
document.querySelectorAll('.bg-grid-dark')
```

### Network (F12 → Network)
1. Откройте вкладку Network
2. Обновите страницу (Ctrl+Shift+R)
3. Проверьте загружаемые файлы:
   - `index-*.js` - должен быть новый (проверьте дату)
   - `index-*.css` - должен быть новый

## Автоматическая диагностика через скрипты

Все скрипты используют SSH доступ через PuTTY:
- **Сервер:** `5.129.235.52`
- **Пользователь:** `root`
- **Пароль:** `cY7^kCCA_6uQ5S`

Скрипты автоматически:
1. Подключаются к серверу
2. Выполняют команды
3. Копируют файлы при необходимости
4. Пересобирают приложение
5. Проверяют результат

## Рекомендуемый порядок действий

1. **Сначала диагностика:**
   ```powershell
   .\diagnose.ps1
   ```

2. **Если есть проблемы - исправление:**
   ```powershell
   .\fix-and-deploy.ps1
   ```

3. **Проверка результата:**
   ```powershell
   .\deep-diagnose.ps1
   ```

4. **Если все еще не работает:**
   ```powershell
   .\final-fix.ps1
   ```

## Важные файлы для проверки

- `frontend/src/components/Layout.tsx` - должен содержать `bg-grid-dark`
- `frontend/src/components/Logo.tsx` - должен экспортировать Logo
- `frontend/src/components/Sidebar.tsx` - должен импортировать Logo
- `frontend/src/index.css` - должен содержать `.bg-grid-dark`
- `frontend/tailwind.config.js` - должен содержать шрифты Inter и JetBrains Mono

## Контакты и поддержка

При возникновении проблем:
1. Запустите диагностику
2. Сохраните вывод скриптов
3. Проверьте логи на сервере: `/var/log/nginx/pentest.red.error.log`

