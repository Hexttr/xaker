# Настройка VPN для работы с Anthropic API

## 🔴 Проблема

На территории РФ доступ к Anthropic API (Cloud) закрыт. При использовании VPN могут возникать проблемы с подключением.

## ✅ Решения

### Вариант 1: Проверка VPN (рекомендуется)

1. **Убедитесь, что VPN активен:**
   - Проверьте статус VPN в системе
   - Убедитесь, что VPN работает и подключен

2. **Проверьте доступность API:**
   ```powershell
   Invoke-WebRequest -Uri "https://api.anthropic.com" -Method GET
   ```
   Если получаете ответ - VPN работает.

3. **Проверьте IP адрес:**
   ```powershell
   Invoke-RestMethod -Uri "https://api.ipify.org?format=json"
   ```
   IP должен быть не из РФ.

### Вариант 2: Настройка прокси через переменные окружения

Если VPN работает через прокси, настройте переменные окружения:

1. **В backend/.env добавьте:**
   ```
   HTTP_PROXY=http://proxy-server:port
   HTTPS_PROXY=http://proxy-server:port
   ```
   Или если прокси требует аутентификацию:
   ```
   HTTP_PROXY=http://username:password@proxy-server:port
   HTTPS_PROXY=http://username:password@proxy-server:port
   ```

2. **Перезапустите backend**

### Вариант 3: Системные настройки прокси

1. Настройте системный прокси в Windows:
   - Настройки → Сеть и Интернет → Прокси
   - Включите "Использовать прокси-сервер"
   - Укажите адрес и порт VPN прокси

2. Node.js автоматически использует системные настройки прокси

### Вариант 4: Использование VPN с режимом TUN/TAP

Некоторые VPN (например, OpenVPN) используют TUN/TAP интерфейс, который работает на уровне системы и должен работать автоматически.

## 🔍 Диагностика

### Проверка доступности API

```powershell
# Проверка доступности
Invoke-WebRequest -Uri "https://api.anthropic.com" -Method GET

# Проверка IP
Invoke-RestMethod -Uri "https://api.ipify.org?format=json"
```

### Проверка прокси

```powershell
# Проверка системных настроек прокси
[System.Net.WebRequest]::GetSystemWebProxy().GetProxy([System.Uri]"https://api.anthropic.com")
```

## ⚠️ Важно

- Убедитесь, что VPN действительно работает и обходит блокировку
- Некоторые VPN могут не работать с API запросами
- Попробуйте другой VPN сервер или провайдера
- Проверьте, что VPN не блокирует HTTPS трафик

## 📋 Чек-лист

- [ ] VPN активен и подключен
- [ ] IP адрес не из РФ (проверено через ipify.org)
- [ ] API доступен (проверено через Invoke-WebRequest)
- [ ] Переменные окружения прокси настроены (если нужно)
- [ ] Backend перезапущен после изменений

