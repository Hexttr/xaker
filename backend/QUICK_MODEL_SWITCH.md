# Быстрое переключение между моделями Claude

## 🚀 Быстрое переключение (1 команда)

```powershell
cd backend
.\switch-claude-model.ps1 [haiku|sonnet|sonnet45]
```

### Примеры:

```powershell
# Переключиться на Claude 4.5 Sonnet (самая умная)
.\switch-claude-model.ps1 sonnet45

# Переключиться на Claude 3.5 Sonnet (баланс)
.\switch-claude-model.ps1 sonnet

# Переключиться на Claude 3 Haiku (дешевая)
.\switch-claude-model.ps1 haiku
```

## 📊 Сравнение моделей

| Модель | Команда | Стоимость | Качество | Рекомендация |
|--------|---------|-----------|----------|--------------|
| **Claude 3 Haiku** | `haiku` | ~$0.80/$4 | Базовое | Для тестовых пентестов |
| **Claude 3.5 Sonnet** | `sonnet` | ~$3/$15 | Высокое | Баланс качества/цены |
| **Claude 4.5 Sonnet** | `sonnet45` | ~$3/$15 | Максимальное | Для важных пентестов ✅ |

## ⚙️ Что происходит при переключении

1. Скрипт обновляет переменную `CLAUDE_MODEL` в файле `.env`
2. Модель применяется для:
   - **Shannon** - выполнение пентеста
   - **Генерация отчетов** - создание PDF отчетов

## ⚠️ После переключения

**ОБЯЗАТЕЛЬНО перезапустите backend:**

```powershell
# Остановите текущий backend (Ctrl+C)
# Затем запустите снова:
cd backend
npm run dev
```

## 💡 Советы

- **Для важных пентестов:** Используйте `sonnet45` (Claude 4.5 Sonnet)
- **Для тестовых/разовых:** Используйте `haiku` (Claude 3 Haiku) для экономии
- **Для баланса:** Используйте `sonnet` (Claude 3.5 Sonnet)

## 🔍 Проверка текущей модели

После переключения модель будет видна в логах backend при запуске пентеста:
```
🔍 Финальная проверка env для Shannon:
   CLAUDE_MODEL: claude-sonnet-4-5-20250929
```

