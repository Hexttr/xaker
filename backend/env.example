# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (для CORS, опционально для разработки)
# FRONTEND_URL=http://localhost:5173

# AI Provider Selection
# Использовать локальный MiroMind вместо Claude API
USE_MIROMIND=false  # true для MiroMind, false для Claude

# MiroMind Configuration (если USE_MIROMIND=true)
MIROMIND_API_URL=http://localhost:8000/v1
MIROMIND_MODEL=mirothinker-8b
MIROMIND_API_KEY=not-needed  # Обычно не требуется

# Claude Configuration (если USE_MIROMIND=false или как fallback)
ANTHROPIC_API_KEY=your_api_key_here
# Если ANTHROPIC_API_KEY не установлен, будет использоваться симуляция

# Claude Model Selection (для экономии средств)
# Доступные модели (от дешевых к дорогим):
# - claude-3-haiku-20240307 (legacy) - ~$0.80/$4 за млн токенов (РЕКОМЕНДУЕТСЯ для экономии)
# - claude-3-5-haiku-20241022 - ~$1/$5 за млн токенов
# - claude-3-5-sonnet-20241022 - ~$3/$15 за млн токенов (баланс качества/цены)
# - claude-sonnet-4-5-20250929 - ~$3/$15 за млн токенов (САМАЯ УМНАЯ, рекомендуется для важных пентестов)
# 
# БЫСТРОЕ ПЕРЕКЛЮЧЕНИЕ: используйте скрипт switch-claude-model.sh (Linux/Mac) или switch-claude-model.ps1 (Windows)
#   ./switch-claude-model.sh haiku    - Claude 3 Haiku (дешевая)
#   ./switch-claude-model.sh sonnet   - Claude 3.5 Sonnet
#   ./switch-claude-model.sh sonnet45 - Claude 4.5 Sonnet (самая умная)
CLAUDE_MODEL=claude-sonnet-4-5-20250929  # Используется для Shannon и генерации отчетов

# Shannon Configuration
# Путь к директории Shannon (по умолчанию ../shannon относительно backend)
# Если Shannon установлен в другом месте, укажите полный путь:
# SHANNON_PATH=/root/shannon
# или
# SHANNON_PATH=/opt/shannon

# Proxy Configuration (для Claude API через VPN)
# HTTP_PROXY=http://127.0.0.1:12334
# HTTPS_PROXY=http://127.0.0.1:12334

# Telegram Notifications (для уведомлений о новых заявках)
# Получить токен: @BotFather в Telegram, создать бота командой /newbot
# Получить chat_id: отправить сообщение боту, затем https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
# Для одного чата (старый формат):
TELEGRAM_CHAT_ID=your_telegram_chat_id_here
# Для нескольких чатов (новый формат, через запятую):
# TELEGRAM_CHAT_IDS=chat_id1,chat_id2,chat_id3

# JWT Authentication
# Секретный ключ для подписи JWT токенов (ОБЯЗАТЕЛЬНО измените в production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Deploy Token (для автоматического деплоя через API)
# Использование: POST /api/deploy с заголовком X-Deploy-Token
# DEPLOY_TOKEN=your-super-secret-deploy-token-change-in-production
