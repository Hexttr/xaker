# 🔐 План реализации аутентификации

## Рекомендуемый подход: **JWT токены с bcrypt**

### Архитектура:

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│   Frontend  │ ──────> │    Backend   │ ──────> │  users.json │
│  (React)    │ <────── │   (Express)  │ <────── │  (bcrypt)   │
└─────────────┘         └──────────────┘         └─────────────┘
     │                         │
     │ JWT Token                │ Middleware
     │ localStorage             │ проверка токена
     └─────────────────────────┘
```

### Компоненты:

1. **Backend:**
   - `/api/auth/login` - POST (username, password) → JWT token
   - `/api/auth/verify` - GET (проверка токена)
   - `/api/auth/logout` - POST (опционально)
   - Middleware `authMiddleware` для защиты роутов
   - Файл `users.json` с хешами паролей (bcrypt)

2. **Frontend:**
   - Компонент `LoginModal` (как RequestDemoModal)
   - Context/Store для хранения токена
   - Компонент `ProtectedRoute` для защиты роутов
   - Автоматический редирект на логин при отсутствии токена

### Безопасность:

✅ **Хранение паролей:**
- bcrypt хеширование (salt rounds: 10-12)
- Никогда не хранить пароли в открытом виде
- Файл `users.json` только для чтения сервером

✅ **JWT токены:**
- Секретный ключ в `.env` (JWT_SECRET)
- Короткое время жизни (1-24 часа)
- Refresh tokens (опционально, для долгих сессий)

✅ **Защита роутов:**
- Middleware проверяет токен на каждом запросе к `/api/*`
- Frontend проверяет токен перед рендером защищенных страниц
- Редирект на логин при невалидном токене

✅ **Дополнительно:**
- Rate limiting для `/api/auth/login` (защита от брутфорса)
- HTTPS обязательно в production
- CORS настройки для безопасности

### Структура файлов:

```
backend/
  src/
    routes/
      auth.routes.ts      # /api/auth/*
    middleware/
      auth.middleware.ts  # Проверка JWT токена
    services/
      auth.service.ts     # Логика аутентификации
    utils/
      password.util.ts   # Хеширование паролей
  users.json              # Хранилище пользователей (gitignored)
```

### Пример users.json:

```json
[
  {
    "id": "1",
    "username": "admin",
    "passwordHash": "$2b$10$...",  // bcrypt hash
    "createdAt": "2026-01-24T10:00:00Z"
  }
]
```

### Поток аутентификации:

1. Пользователь открывает `/app`
2. Frontend проверяет наличие токена
3. Если нет → показываем LoginModal
4. Пользователь вводит логин/пароль
5. POST `/api/auth/login` с credentials
6. Backend проверяет пароль (bcrypt.compare)
7. Если верно → генерируем JWT токен
8. Возвращаем токен клиенту
9. Клиент сохраняет токен в localStorage
10. Редирект на `/app` (теперь доступ открыт)

### Защита API:

```typescript
// Все роуты /api/pentests, /api/services требуют токен
app.use('/api/pentests', authMiddleware, pentestRoutes);
app.use('/api/services', authMiddleware, serviceRoutes);
```

### Управление пользователями:

- Скрипт для добавления пользователей: `add-user.ps1`
- Пароли хешируются при добавлении
- Можно редактировать `users.json` вручную (с перехешированием)

---

## Альтернативные варианты:

### Вариант 2: Сессии (express-session)
- ✅ HttpOnly cookies (защита от XSS)
- ❌ Нужен Redis или файловое хранилище
- ❌ Сложнее масштабировать

### Вариант 3: JWT в HttpOnly cookies
- ✅ Лучшая защита от XSS
- ❌ Сложнее настройка CORS
- ❌ Нужны дополнительные настройки

---

## Рекомендация: **Вариант 1 (JWT + localStorage)**

**Почему:**
- Простота реализации
- Не требует дополнительных зависимостей (Redis)
- Легко масштабировать
- Достаточно безопасно для внутреннего использования
- Можно добавить refresh tokens позже

**Меры безопасности:**
- HTTPS обязательно
- Короткое время жизни токенов
- Rate limiting на логин
- Валидация токена на каждом запросе

