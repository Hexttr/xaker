# 📦 Инструкция по установке Xaker на Windows

## 📋 Требования

### Системные требования:
- **ОС:** Windows 10/11 (64-bit)
- **Node.js:** v18.0.0 или выше
- **npm:** v9.0.0 или выше
- **Git:** для клонирования репозитория
- **PowerShell:** 7.0 или выше (для скриптов)

### Дополнительно:
- **Anthropic API ключ** (для реальных пентестов)
- **VPN** (если находитесь в РФ, для доступа к Anthropic API)
- **8GB+ RAM** (рекомендуется)
- **5GB+ свободного места** на диске

---

## 🚀 Быстрая установка

### Шаг 1: Проверка системных требований

Откройте PowerShell и выполните:

```powershell
# Проверка Node.js
node --version
# Должно быть: v18.0.0 или выше

# Проверка npm
npm --version
# Должно быть: v9.0.0 или выше

# Проверка Git
git --version
```

Если чего-то нет, установите:
- **Node.js:** https://nodejs.org/
- **Git:** https://git-scm.com/download/win

### Шаг 2: Настройка PowerShell (если нужно)

Если при выполнении скриптов возникает ошибка о политике выполнения:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Шаг 3: Клонирование репозитория

```powershell
cd C:\
git clone https://github.com/Hexttr/xaker.git
cd xaker
```

### Шаг 4: Установка зависимостей

```powershell
# Установка зависимостей для всех компонентов
npm run install:all
```

Или вручную:

```powershell
# Backend
cd backend
npm install
cd ..

# Frontend
cd frontend
npm install
cd ..
```

### Шаг 5: Клонирование Shannon

Shannon - это ядро AI пентестера:

```powershell
# Клонируем Shannon в папку shannon
cd ..
git clone https://github.com/KeygraphHQ/shannon.git xaker\shannon
cd xaker\shannon

# Установка зависимостей Shannon
npm install

# Сборка Shannon
npm run build

# Сборка mcp-server (подпроект Shannon)
cd mcp-server
npm install
npm run build
cd ..
```

### Шаг 6: Настройка окружения

Создайте файл `backend/.env`:

```powershell
cd ..\backend
```

Создайте файл `.env` со следующим содержимым:

```env
# Backend конфигурация
PORT=3000
FRONTEND_URL=http://localhost:5173

# Anthropic API (для реальных пентестов)
ANTHROPIC_API_KEY=your_api_key_here

# Режим симуляции (true = безопасное тестирование без API)
USE_SIMULATION=false

# Прокси для VPN (если нужно)
# HTTP_PROXY=http://127.0.0.1:12334
# HTTPS_PROXY=http://127.0.0.1:12334
```

**Важно:** Замените `your_api_key_here` на ваш реальный API ключ от Anthropic.

### Шаг 7: Сборка проекта

```powershell
# Возвращаемся в корень проекта
cd ..

# Сборка backend
cd backend
npm run build
cd ..
```

### Шаг 8: Запуск

#### Вариант 1: Использование скриптов (рекомендуется)

```powershell
# Запуск всего (backend + frontend)
.\start-all.ps1
```

Или отдельно:

```powershell
# Backend (в одном окне PowerShell)
.\start-backend.ps1

# Frontend (в другом окне PowerShell)
.\start-frontend.ps1
```

#### Вариант 2: Ручной запуск

```powershell
# Backend
cd backend
npm run dev

# Frontend (в другом терминале)
cd frontend
npm run dev
```

### Шаг 9: Проверка работоспособности

1. **Backend:** Откройте http://localhost:3000/api/health
   - Должен вернуть: `{"status":"ok",...}`

2. **Frontend:** Откройте http://localhost:5173
   - Должна открыться веб-страница с интерфейсом

---

## 🔧 Дополнительная настройка

### Настройка Anthropic API ключа

1. Получите API ключ на https://console.anthropic.com
2. Откройте `backend/.env`
3. Замените `ANTHROPIC_API_KEY=your_api_key_here` на ваш ключ
4. Перезапустите backend

### Настройка VPN (для РФ)

Если вы находитесь в РФ, нужен VPN для доступа к Anthropic API:

1. Включите VPN
2. Проверьте IP адрес: https://api.ipify.org
3. IP должен быть не из РФ
4. Если VPN использует прокси, добавьте в `.env`:
   ```
   HTTP_PROXY=http://proxy:port
   HTTPS_PROXY=http://proxy:port
   ```

### Режим симуляции

Для безопасного тестирования без затрат API:

```env
USE_SIMULATION=true
```

В этом режиме:
- ✅ Не используются реальные API вызовы
- ✅ Не тратятся деньги
- ✅ Можно протестировать весь интерфейс
- ❌ Реальные пентесты не выполняются

---

## 📁 Структура проекта

```
xaker/
├── backend/              # Backend API (Node.js + Express + TypeScript)
│   ├── src/
│   │   ├── server.ts    # Главный сервер
│   │   ├── routes/      # API маршруты
│   │   ├── services/    # Сервисы (Shannon интеграция)
│   │   └── types/       # TypeScript типы
│   ├── .env            # Конфигурация (создать вручную)
│   └── package.json
│
├── frontend/            # Frontend (React + Vite + Tailwind)
│   ├── src/
│   │   ├── pages/      # Страницы
│   │   ├── services/   # API клиент
│   │   └── App.tsx
│   └── package.json
│
├── shannon/            # Shannon AI Pentester (клонировать отдельно)
│   ├── dist/          # Скомпилированный код
│   └── ...
│
├── start-backend.ps1   # Скрипт запуска backend
├── start-frontend.ps1  # Скрипт запуска frontend
├── start-all.ps1       # Скрипт запуска всего
├── stop-all.ps1        # Скрипт остановки
│
└── README.md
```

---

## 🐛 Решение проблем

### Проблема: PowerShell не выполняет скрипты

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: Порт 3000 или 5173 занят

**Решение:**
```powershell
# Найти процесс на порту 3000
netstat -ano | findstr :3000

# Убить процесс (замените PID)
taskkill /PID <PID> /F
```

Или измените порт в `.env`:
```env
PORT=3001
```

### Проблема: Ошибка при установке зависимостей

**Решение:**
```powershell
# Очистка кэша npm
npm cache clean --force

# Удаление node_modules и package-lock.json
Remove-Item -Recurse -Force node_modules, package-lock.json
Remove-Item -Recurse -Force backend\node_modules, backend\package-lock.json
Remove-Item -Recurse -Force frontend\node_modules, frontend\package-lock.json

# Переустановка
npm run install:all
```

### Проблема: Shannon не найден

**Решение:**
1. Убедитесь, что Shannon клонирован в `shannon/`
2. Убедитесь, что выполнен `npm run build` в `shannon/`
3. Проверьте путь в `backend/src/services/shannon.service.ts`

### Проблема: Ошибка 403 от Anthropic API

**Возможные причины:**
1. Неверный API ключ
2. Недостаточно средств на балансе
3. VPN не работает (для РФ)
4. IP заблокирован

**Решение:**
1. Проверьте API ключ на https://console.anthropic.com
2. Проверьте баланс
3. Проверьте VPN (IP должен быть не из РФ)
4. Используйте режим симуляции для тестирования

### Проблема: Ошибка при сборке Shannon

**Решение:**
```powershell
cd shannon\mcp-server
npm install
npm run build
cd ..
npm run build
```

---

## ✅ Проверка установки

После установки выполните проверку:

```powershell
# 1. Проверка структуры проекта
Test-Path backend
Test-Path frontend
Test-Path shannon

# 2. Проверка зависимостей
Test-Path backend\node_modules
Test-Path frontend\node_modules
Test-Path shannon\node_modules

# 3. Проверка сборки
Test-Path backend\dist
Test-Path shannon\dist

# 4. Проверка конфигурации
Test-Path backend\.env

# 5. Запуск и проверка API
Start-Process powershell -ArgumentList "-NoExit", "-File", "$PWD\start-backend.ps1"
Start-Sleep -Seconds 5
Invoke-WebRequest http://localhost:3000/api/health
```

Все проверки должны пройти успешно!

---

## 🚀 Быстрый старт (краткая версия)

```powershell
# 1. Клонирование
cd C:\
git clone https://github.com/Hexttr/xaker.git
cd xaker

# 2. Установка зависимостей
npm run install:all

# 3. Клонирование Shannon
cd ..
git clone https://github.com/KeygraphHQ/shannon.git xaker\shannon
cd xaker\shannon
npm install
npm run build
cd mcp-server
npm install
npm run build
cd ..\..

# 4. Настройка
cd xaker\backend
# Создайте .env файл (см. выше)
cd ..

# 5. Запуск
.\start-all.ps1
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи в окнах PowerShell
3. Создайте issue в репозитории: https://github.com/Hexttr/xaker/issues

---

## 📝 Примечания

- **Первый запуск** может занять несколько минут (сборка, установка)
- **Реальные пентесты** требуют валидный Anthropic API ключ
- **VPN необходим** если вы в РФ и хотите использовать реальные пентесты
- **Режим симуляции** позволяет протестировать интерфейс без затрат

---

## 🎯 Следующие шаги

После успешной установки:

1. Откройте http://localhost:5173
2. Создайте тестовый пентест
3. Попробуйте запустить (с `USE_SIMULATION=true` для безопасности)
4. Изучите результаты

Удачи! 🚀




