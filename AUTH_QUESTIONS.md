# ❓ Вопросы перед реализацией JWT аутентификации

## 📋 Вопросы для обсуждения:

### 1. **Хранение пользователей**
- ✅ **Вариант A:** `backend/users.json` (простой JSON файл)
- ❓ **Вариант B:** Отдельная папка `backend/data/users.json`
- ❓ **Вариант C:** В `.env` (только для одного пользователя)

**Рекомендация:** Вариант A - `backend/users.json` (просто, легко редактировать)

---

### 2. **Управление пользователями**
- ❓ Как вы будете добавлять/редактировать пользователей?
  - **A:** Скрипт `add-user.ps1` (автоматическое хеширование паролей)
  - **B:** Ручное редактирование `users.json` + скрипт для хеширования
  - **C:** Через API endpoint (требует админ-токен)

**Рекомендация:** Вариант A - скрипт для удобства

---

### 3. **Время жизни JWT токена**
- ❓ Сколько часов должен жить токен?
  - **A:** 1 час (короткий, безопаснее)
  - **B:** 6 часов (удобнее для пользователя)
  - **C:** 24 часа (очень удобно, но менее безопасно)

**Рекомендация:** Вариант B - 6 часов (удобно для работы с пентестами)

---

### 4. **Refresh Token механизм**
- ❓ Нужен ли refresh token для продления сессии?
  - **A:** Да, с sliding session (6 часов бездействия)
  - **B:** Нет, просто JWT на 6 часов (проще)

**Рекомендация:** Вариант B - пока без refresh token (проще, достаточно для одной компании)

---

### 5. **Опция "Запомнить меня"**
- ❓ Нужна ли опция "Запомнить меня"?
  - **A:** Да (токен на 30 дней)
  - **B:** Нет (всегда 6 часов)

**Рекомендация:** Вариант B - пока не нужна (одна компания)

---

### 6. **JWT_SECRET**
- ❓ Где хранить секретный ключ?
  - **A:** В `.env` файле (JWT_SECRET=...)
  - **B:** Генерировать автоматически при первом запуске

**Рекомендация:** Вариант A - в `.env` (контролируемо)

---

### 7. **Защита роутов**
- ❓ Какие роуты защищать?
  - **A:** Все `/api/*` кроме `/api/auth/*` и `/api/health`
  - **B:** Только `/api/pentests/*`, `/api/services/*`
  - **C:** Все `/api/*` включая `/api/demo-requests` (публичный)

**Рекомендация:** Вариант A - все API кроме auth и health

---

### 8. **Frontend: LoginModal**
- ❓ Где показывать форму логина?
  - **A:** Модальное окно (как RequestDemoModal) при попытке доступа к `/app`
  - **B:** Отдельная страница `/login` с редиректом
  - **C:** Модальное окно в Header (кнопка "Login")

**Рекомендация:** Вариант A - модальное окно при доступе к `/app`

---

### 9. **Редирект после логина**
- ❓ Куда редиректить после успешного логина?
  - **A:** На `/app` (главная страница)
  - **B:** На `/app/pentests` (пентесты)
  - **C:** На страницу, с которой пытались зайти

**Рекомендация:** Вариант C - на страницу, с которой пытались зайти

---

### 10. **Хранение токена на Frontend**
- ❓ Где хранить JWT токен?
  - **A:** `localStorage` (просто, но риск XSS)
  - **B:** `sessionStorage` (очищается при закрытии вкладки)
  - **C:** В памяти (нужно логиниться при каждой перезагрузке)

**Рекомендация:** Вариант A - `localStorage` (удобно, для одной компании безопасно)

---

### 11. **Валидация токена**
- ❓ Когда проверять валидность токена?
  - **A:** При каждом API запросе (backend middleware)
  - **B:** При загрузке приложения + при каждом запросе
  - **C:** Только при запросах к защищенным роутам

**Рекомендация:** Вариант B - при загрузке + при каждом запросе

---

### 12. **Обработка истекшего токена**
- ❓ Что делать, если токен истек?
  - **A:** Автоматически показывать LoginModal
  - **B:** Редирект на `/login` страницу
  - **C:** Показывать уведомление "Сессия истекла"

**Рекомендация:** Вариант A - автоматически показывать LoginModal

---

### 13. **Структура users.json**
- ❓ Какие поля хранить?
  ```json
  [
    {
      "id": "uuid",
      "username": "admin",
      "passwordHash": "$2b$10$...",
      "createdAt": "2026-01-24T..."
    }
  ]
  ```
  - ❓ Нужны ли дополнительные поля? (email, role, etc.)

**Рекомендация:** Минимальная структура (id, username, passwordHash, createdAt)

---

### 14. **Безопасность паролей**
- ❓ Требования к паролям?
  - **A:** Минимум 8 символов
  - **B:** Минимум 12 символов
  - **C:** Без ограничений (вы сами задаете)

**Рекомендация:** Вариант C - без ограничений (вы контролируете)

---

### 15. **Rate Limiting**
- ❓ Нужна ли защита от брутфорса на `/api/auth/login`?
  - **A:** Да, максимум 5 попыток в минуту
  - **B:** Нет, пока не нужна (одна компания)

**Рекомендация:** Вариант A - базовая защита (просто реализовать)

---

## 📝 Рекомендуемая конфигурация (по умолчанию):

Если не указано иное, буду использовать:

1. ✅ `backend/users.json` для хранения пользователей
2. ✅ Скрипт `add-user.ps1` для управления
3. ✅ JWT токен на 6 часов
4. ✅ Без refresh token (пока не нужен)
5. ✅ JWT_SECRET в `.env`
6. ✅ Защита всех `/api/*` кроме `/api/auth/*` и `/api/health`
7. ✅ LoginModal при доступе к `/app`
8. ✅ Редирект на страницу, с которой пытались зайти
9. ✅ `localStorage` для хранения токена
10. ✅ Проверка токена при загрузке + при каждом запросе
11. ✅ Автоматический показ LoginModal при истекшем токене
12. ✅ Минимальная структура users.json
13. ✅ Без ограничений на пароли
14. ✅ Rate limiting: 5 попыток в минуту

---

## 🚀 Готов начать после ваших ответов!

