# 🚀 Инструменты для быстрого деплоя

## Проблема
Копирование файлов через PowerShell/scp вызывает проблемы с экранированием и путями.

## Решение - используйте эти скрипты:

### 1. `quick-deploy.ps1` - Быстрый деплой через Git (РЕКОМЕНДУЕТСЯ)
```powershell
# Деплой лендинга
.\quick-deploy.ps1 -Type landing

# Деплой бэкенда
.\quick-deploy.ps1 -Type backend

# Деплой всего
.\quick-deploy.ps1 -Type all
```

**Как работает:**
- Коммитит все изменения в Git
- Пушит в prod ветку
- На сервере делает git pull, build, deploy

### 2. `edit-server-file.ps1` - Быстрое редактирование одного файла
```powershell
# Обновить файл на сервере
.\edit-server-file.ps1 -Path "/opt/xaker/landing/src/components/Header.tsx" -LocalFile "Header.tsx"
```

**Как работает:**
- Копирует локальный файл на сервер через scp
- Быстро и просто для одного файла

## Рекомендуемый workflow:

1. **Редактируйте файлы локально** в C:\Xaker или C:\Xakerprod
2. **Используйте Git для синхронизации:**
   ```powershell
   cd C:\Xaker
   git add .
   git commit -m "Описание изменений"
   git push origin prod
   ```
3. **Деплой через скрипт:**
   ```powershell
   .\quick-deploy.ps1 -Type landing  # или backend, или all
   ```

## Альтернатива - прямое редактирование на сервере:

Если нужно быстро исправить что-то на сервере:
```powershell
ssh root@pentest.red
cd /opt/xaker/landing/src/components
nano Header.tsx  # или vim
# После редактирования:
cd /opt/xaker/landing && npm run build && cp -r dist/* /var/www/pentest.red/landing/
```

## Важно:
- **Всегда используйте Git** для важных изменений
- **Быстрые правки** можно делать через `edit-server-file.ps1`
- **Массовые изменения** - через `quick-deploy.ps1`

