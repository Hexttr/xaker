# План рефакторинга проекта

## 🎯 Цели
1. Удалить неиспользуемый код и файлы
2. Организовать структуру проекта
3. Улучшить читаемость кода
4. Сохранить работоспособность приложения

## 📋 Этапы рефакторинга

### Этап 1: Удаление дубликатов и неиспользуемых файлов ⚠️ БЕЗОПАСНО

#### 1.1 Удаление дубликатов сервисов
- ❌ `backend/src/services/shanon.service.ts` - дубликат (используется `shannon.service.ts`)
- ✅ Проверено: не импортируется нигде

#### 1.2 Удаление тестовых/старых серверов
- ❌ `backend/src/server-simple.ts` - упрощенная версия, не используется
- ❌ `backend/src/test-server.ts` - тестовый сервер, не используется в продакшене
- ✅ Проверено: не используются в package.json scripts

#### 1.3 Удаление старых скриптов
- ❌ `backend/generate-pdf.js` - старый скрипт (функционал в `pdfReport.service.ts`)
- ✅ Проверено: функционал перенесен в сервис

#### 1.4 Очистка вложенных папок
- ❌ `backend/backend/backend/` - странная вложенность (возможно копии)
- ⚠️ Проверить содержимое перед удалением
- ✅ Переместить важные файлы (если есть) в правильные места

#### 1.5 Удаление временных файлов
- ❌ `backend/temp-repo/` - временная папка для тестов
- ❌ `backend/test5-report.pdf` - тестовый отчет
- ❌ `backend/КРАТКИЙ_ОТЧЕТ_ПЕНТЕСТ_TCELL_FINAL.md` - переместить в `backend/reports/` или удалить

### Этап 2: Организация документации 📚

#### 2.1 Создание структуры docs/
```
docs/
  ├── setup/          # Инструкции по установке
  │   ├── INSTALL.md
  │   ├── SETUP.md
  │   └── QUICKSTART.md
  ├── guides/         # Руководства
  │   ├── API_SETUP.md
  │   ├── VPN_SETUP.md
  │   └── TROUBLESHOOTING.md
  ├── development/    # Документация для разработчиков
  │   ├── INTEGRATION_PLAN.md
  │   ├── IMPLEMENTATION_STATUS.md
  │   └── NEXT_STEPS.md
  └── reports/        # Отчеты и анализы
      ├── PENTEST_REPORT_*.md
      └── VULNERABILITIES_*.md
```

#### 2.2 Файлы для перемещения
- Все `.md` файлы из корня в соответствующие папки `docs/`
- Оставить только `README.md` в корне

### Этап 3: Очистка кода 🧹

#### 3.1 Удаление TODO комментариев (или их реализация)
- `backend/src/services/shannon.service.ts:377` - TODO: Создать временный YAML конфиг
- `backend/src/services/shanon.service.ts:34` - TODO: Здесь будет реальный вызов Shannon (файл удаляется)

#### 3.2 Удаление неиспользуемых импортов
- Проверить все файлы на неиспользуемые импорты
- Использовать ESLint для автоматической проверки

#### 3.3 Удаление закомментированного кода
- Найти и удалить закомментированные блоки кода
- Если код важен - добавить комментарий почему он закомментирован

### Этап 4: Оптимизация структуры 📁

#### 4.1 Организация папок backend
```
backend/
  ├── src/
  │   ├── routes/        ✅ Уже есть
  │   ├── services/       ✅ Уже есть
  │   ├── types/         ✅ Уже есть
  │   ├── utils/         ✅ Уже есть
  │   └── middleware/    ➕ Создать для middleware
  ├── data/              ✅ Уже есть (services.json)
  ├── reports/           ✅ Уже есть
  ├── pentests/          ✅ Уже есть
  └── dist/              ✅ Автогенерируется
```

#### 4.2 Очистка dist/
- Добавить `dist/` в `.gitignore` (если еще нет)
- Удалить `dist/` из git (если был закоммичен)

### Этап 5: Улучшение конфигурации ⚙️

#### 5.1 Обновление .gitignore
- Убедиться, что все временные файлы игнорируются
- Добавить `*.log`, `*.pdf` (кроме важных), `temp-*`, `test-*`

#### 5.2 Очистка package.json
- Проверить неиспользуемые зависимости
- Удалить неиспользуемые скрипты

## 🔒 Безопасность рефакторинга

### Перед началом:
1. ✅ Создать бэкап (git commit уже есть)
2. ✅ Проверить, что приложение работает
3. ✅ Убедиться, что все тесты проходят (если есть)

### Процесс:
1. Удалять файлы по одному
2. После каждого удаления проверять, что приложение запускается
3. Коммитить изменения после каждого этапа

### После рефакторинга:
1. Запустить приложение
2. Проверить основные функции
3. Убедиться, что нет ошибок в консоли

## 📊 Ожидаемый результат

### Удалено:
- ~5-10 неиспользуемых файлов
- ~50+ строк закомментированного кода
- Дубликаты и временные файлы

### Улучшено:
- Структура проекта более понятная
- Документация организована
- Код чище и читабельнее

### Сохранено:
- ✅ Вся функциональность работает
- ✅ Все важные файлы сохранены
- ✅ История git сохранена

## 🚀 Порядок выполнения

1. **Этап 1** - Удаление дубликатов (самый безопасный)
2. **Этап 2** - Организация документации
3. **Этап 3** - Очистка кода
4. **Этап 4** - Оптимизация структуры
5. **Этап 5** - Улучшение конфигурации

## ⚠️ Внимание

- НЕ удалять файлы из `shannon/` - это внешняя библиотека
- НЕ удалять файлы из `miromind/` - это внешний сервис
- НЕ удалять `node_modules/` - они в .gitignore
- НЕ удалять важные отчеты - перемещать в `docs/reports/`

