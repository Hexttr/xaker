# Статус сборки Shannon

## ✅ Что сделано

1. ✅ API ключ добавлен в `backend/.env`
2. ✅ Зависимости Shannon установлены
3. ⏳ Сборка mcp-server (в процессе)

## ⚠️ Если есть ошибки сборки

Shannon требует сборки нескольких компонентов:

1. **mcp-server** (должен быть собран первым)
   ```bash
   cd shannon/mcp-server
   npm install
   npm run build
   ```

2. **Основной Shannon**
   ```bash
   cd shannon
   npm install
   npm run build
   ```

## 🔄 Альтернатива: Использование без сборки

Если сборка не работает, можно использовать Docker версию Shannon (см. `shannon/README.md`).

Но для нашей интеграции лучше собрать локально.

## 📝 После успешной сборки

1. Перезапустить backend
2. Создать пентест
3. Запустить - должны появиться реальные логи от Shannon!








