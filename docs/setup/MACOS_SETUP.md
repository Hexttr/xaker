# 📦 Инструкция по установке Xaker на macOS

## 📋 Требования

### Системные требования:
- **ОС:** macOS 10.15 (Catalina) или выше
- **Node.js:** v18.0.0 или выше
- **npm:** v9.0.0 или выше
- **Git:** для клонирования репозитория
- **Terminal:** встроенный терминал macOS

### Дополнительно:
- **Anthropic API ключ** (для реальных пентестов)
- **VPN** (если находитесь в РФ, для доступа к Anthropic API)
- **8GB+ RAM** (рекомендуется)
- **5GB+ свободного места** на диске

---

## 🚀 Быстрая установка

### Шаг 1: Проверка системных требований

Откройте Terminal (Приложения → Утилиты → Терминал) и выполните:

```bash
# Проверка Node.js
node --version
# Должно быть: v18.0.0 или выше

# Проверка npm
npm --version
# Должно быть: v9.0.0 или выше

# Проверка Git
git --version
```

Если чего-то нет, установите:

#### Установка Node.js:
1. Перейдите на https://nodejs.org/
2. Скачайте LTS версию для macOS
3. Установите через установщик (.pkg файл)

**Или через Homebrew:**
```bash
brew install node
```

#### Установка Git:
Git обычно уже установлен в macOS. Если нет:
```bash
# Через Homebrew
brew install git

# Или скачайте с https://git-scm.com/download/mac
```

#### Установка Homebrew (если еще не установлен):
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Клонирование репозитория

```bash
# Перейдите в нужную директорию
cd ~/Documents  # или любая другая папка

# Клонируйте репозиторий
git clone <URL_РЕПОЗИТОРИЯ>
cd xaker  # или имя вашей папки
```

### Шаг 3: Установка зависимостей

```bash
# Установка всех зависимостей (корневой проект, backend, frontend)
npm run install:all
```

Это займет несколько минут. Если возникнут ошибки с правами доступа, используйте `sudo` (не рекомендуется) или исправьте права:

```bash
# Исправление прав (если нужно)
sudo chown -R $(whoami) ~/.npm
```

### Шаг 4: Настройка backend

Создайте файл `.env` на основе примера:

```bash
cd backend
cp env.example .env
```

Откройте файл `.env` в любом текстовом редакторе:

```bash
# Через nano (встроенный редактор)
nano .env

# Или через VS Code (если установлен)
code .env

# Или через TextEdit
open -a TextEdit .env
```

Добавьте ваш Anthropic API ключ:

```env
ANTHROPIC_API_KEY=your_api_key_here
```

Сохраните файл:
- В nano: `Ctrl + O` (сохранить), `Enter`, `Ctrl + X` (выйти)
- В VS Code: `Cmd + S`
- В TextEdit: `Cmd + S`

### Шаг 5: Запуск приложения

Вернитесь в корневую директорию проекта:

```bash
cd ..
```

Запустите приложение:

```bash
npm run dev
```

Это запустит одновременно backend и frontend.

**Или запустите отдельно:**

```bash
# В первом окне Terminal - Backend
npm run dev:backend

# Во втором окне Terminal - Frontend
npm run dev:frontend
```

### Шаг 6: Открытие в браузере

После запуска откройте браузер и перейдите:

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:3000

---

## 🛠️ Решение проблем

### Проблема: "Command not found: node"

**Решение:**
```bash
# Проверьте, установлен ли Node.js
which node

# Если не установлен, установите через Homebrew
brew install node

# Или скачайте с nodejs.org
```

### Проблема: "EACCES: permission denied"

**Решение:**
```bash
# Исправление прав на npm директорию
sudo chown -R $(whoami) $(npm config get prefix)/{lib/node_modules,bin,share}
```

### Проблема: "Port 3000 already in use" или "Port 5173 already in use"

**Решение:**
```bash
# Найти процесс, использующий порт
lsof -ti:3000  # для backend
lsof -ti:5173  # для frontend

# Остановить процесс
kill -9 $(lsof -ti:3000)
kill -9 $(lsof -ti:5173)
```

### Проблема: "Cannot find module" после установки

**Решение:**
```bash
# Удалите node_modules и установите заново
rm -rf node_modules backend/node_modules frontend/node_modules
npm run install:all
```

### Проблема: Медленная установка зависимостей

**Решение:**
```bash
# Используйте более быстрый реестр (опционально)
npm config set registry https://registry.npmjs.org/

# Или используйте yarn вместо npm
npm install -g yarn
yarn install
```

---

## 📝 Полезные команды

### Остановка приложения

Нажмите `Ctrl + C` в терминале, где запущено приложение.

### Очистка кэша

```bash
# Очистка npm кэша
npm cache clean --force

# Очистка node_modules
rm -rf node_modules backend/node_modules frontend/node_modules package-lock.json
npm run install:all
```

### Обновление зависимостей

```bash
# Обновление всех зависимостей
npm update
cd backend && npm update && cd ..
cd frontend && npm update && cd ..
```

### Просмотр логов

Логи отображаются в терминале, где запущено приложение. Для более детального просмотра:

```bash
# Backend логи
cd backend
npm run dev

# Frontend логи
cd frontend
npm run dev
```

---

## 🔧 Дополнительная настройка

### Настройка VS Code (опционально)

Если используете VS Code, установите расширения:
- ESLint
- Prettier
- TypeScript и JavaScript Language Features

### Настройка Git (если еще не настроен)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

---

## ✅ Проверка установки

После установки проверьте:

1. ✅ Node.js установлен: `node --version`
2. ✅ npm установлен: `npm --version`
3. ✅ Зависимости установлены: `ls node_modules` (должна быть папка)
4. ✅ Backend .env настроен: `cat backend/.env` (должен содержать ANTHROPIC_API_KEY)
5. ✅ Приложение запускается: `npm run dev` (без ошибок)
6. ✅ Frontend открывается: http://localhost:5173
7. ✅ Backend отвечает: http://localhost:3000/api/services

---

## 🎯 Следующие шаги

1. Изучите [Быстрый старт](../QUICKSTART.md) для базового использования
2. Прочитайте [Руководство по тестированию](../guides/REAL_TEST_GUIDE.md)
3. Ознакомьтесь с [Решением проблем](../guides/TROUBLESHOOTING.md)

---

## 💡 Советы

- Используйте **iTerm2** вместо стандартного Terminal для лучшего опыта
- Установите **Oh My Zsh** для улучшенной работы с терминалом
- Используйте **VS Code** как основной редактор кода
- Настройте **GitHub CLI** для удобной работы с Git

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Изучите [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
3. Создайте Issue в репозитории с описанием проблемы

---

**Готово!** Теперь вы можете использовать Xaker на macOS. 🎉

