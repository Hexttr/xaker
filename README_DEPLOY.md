# 🚀 Деплой Xaker на Ubuntu 24.02

Это краткое руководство по деплою приложения Xaker на сервер Ubuntu 24.02.

## 📋 Быстрый старт

1. **Подключитесь к серверу по SSH**
2. **Клонируйте репозиторий:**
   ```bash
   git clone <URL_РЕПОЗИТОРИЯ> xaker
   cd xaker
   git checkout prod
   ```

3. **Следуйте инструкции:** [docs/setup/UBUNTU_SETUP.md](docs/setup/UBUNTU_SETUP.md)

## 🔑 Ключевые моменты

### Системные зависимости

Обязательно установите зависимости для Puppeteer (см. Шаг 3 в UBUNTU_SETUP.md):
```bash
sudo apt install -y libgbm1 libnss3 libatk-bridge2.0-0 chromium-browser
```

### Переменные окружения

Создайте `.env` файл в `backend/` на основе `env.production.example`:
```bash
cd backend
cp env.production.example .env
nano .env
```

**Обязательно настройте:**
- `FRONTEND_URL` - ваш домен или IP
- `ANTHROPIC_API_KEY` - ключ API Anthropic
- `CLAUDE_MODEL` - модель Claude (для экономии используйте `claude-3-haiku-20240307`)

### Запуск

**Для разработки:**
```bash
chmod +x *.sh backend/*.sh
./start-all.sh
```

**Для продакшена (рекомендуется PM2):**
```bash
npm install -g pm2
cd backend && pm2 start npm --name "xaker-backend" -- run start
pm2 startup
pm2 save
```

### Nginx конфигурация

См. раздел "Настройка Nginx" в [UBUNTU_SETUP.md](docs/setup/UBUNTU_SETUP.md)

## 📝 Чеклист деплоя

- [ ] Установлен Node.js v18+
- [ ] Установлены системные зависимости для Puppeteer
- [ ] Клонирован репозиторий и переключен на ветку `prod`
- [ ] Установлены зависимости проекта (`npm run install:all`)
- [ ] Создан и настроен `.env` файл
- [ ] Проект собран (`npm run build`)
- [ ] Скрипты сделаны исполняемыми (`chmod +x *.sh`)
- [ ] Настроен Nginx (для продакшена)
- [ ] Настроен SSL (Let's Encrypt)
- [ ] Настроен автозапуск (PM2 или systemd)
- [ ] Проверена работа приложения

## 🐛 Решение проблем

См. раздел "Решение проблем" в [UBUNTU_SETUP.md](docs/setup/UBUNTU_SETUP.md)

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи приложения: `pm2 logs` или `sudo journalctl -u xaker-backend`
2. Логи Nginx: `sudo tail -f /var/log/nginx/error.log`
3. Статус сервисов: `pm2 status` или `sudo systemctl status xaker-backend`

